import React from 'react'

export default function Food ({data}) {

  
  
  return (
    <div>

      {/* {data && data.map((foodList, i) => (
        <p key={i}>{foodList.text}</p>
      ))} */}
    </div>
  )
}
